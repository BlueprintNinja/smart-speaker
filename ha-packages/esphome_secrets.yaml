# ESPHome secrets — fill these in before flashing
# Generate a random 32-byte base64 key at: https://esphome.io/components/api.html
esphome_api_key: "REPLACE_WITH_32_BYTE_BASE64_KEY=="
esphome_ota_password: "REPLACE_WITH_OTA_PASSWORD"
